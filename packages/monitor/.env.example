# Argus Monitor Configuration

# Required: Solana RPC endpoint (with WebSocket support)
# Use your Helius endpoint for free WebSocket subscriptions
RPC_ENDPOINT=https://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_KEY

# Optional: Explicit WebSocket endpoint (auto-derived from RPC_ENDPOINT if not set)
# RPC_WS_ENDPOINT=wss://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_KEY

# Optional: Argus Workers API for agent event integration
# WORKERS_API_URL=https://argusguard-api.workers.dev

# Optional: Telegram notifications
# TELEGRAM_BOT_TOKEN=your-bot-token
# TELEGRAM_CHANNEL_ID=-1001234567890

# Optional: Minimum liquidity to analyze (default: 1000)
# MIN_LIQUIDITY_USD=1000

# Optional: Comma-separated list of DEXs to monitor (default: all)
# ENABLED_DEXS=RAYDIUM_CPMM,PUMP_FUN,METEORA_DLMM
